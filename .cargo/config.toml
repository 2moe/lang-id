[alias]
ta = ["test", "--all", "--all-features"]
open-doc = [
    "test",
    # "--package",
    # "lang-id",
    "--test",
    "doc",
    "--all-features",
    "--",
    "build_and_open_rust_doc",
    "--exact",
    "--show-output",
    "--ignored",
]
